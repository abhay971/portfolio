# Vercel Postgres Connection Strings
# Get these from your Vercel Postgres database dashboard
POSTGRES_URL="postgres://default:***@***-pooler.postgres.vercel.com:5432/verceldb?sslmode=require"
POSTGRES_PRISMA_URL="postgres://default:***@***-pooler.postgres.vercel.com:5432/verceldb?pgbouncer=true&connect_timeout=15"
POSTGRES_URL_NON_POOLING="postgres://default:***@***.postgres.vercel.com:5432/verceldb?sslmode=require"

# Resend Email Service
# Get your API key from https://resend.com/
RESEND_API_KEY="re_***"
ADMIN_EMAIL="your.email@example.com"
FROM_EMAIL="Portfolio Contact <noreply@yourdomain.com>"

# JWT Secret
# Generate with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-here-change-in-production"

# Rate Limiting Configuration
RATE_LIMIT_MAX_REQUESTS="3"
RATE_LIMIT_WINDOW_MS="3600000"

# IMPORTANT NOTES:
# 1. Copy this file to .env.local and fill in your actual values
# 2. NEVER commit .env.local to git (it's already in .gitignore)
# 3. Set these same variables in your Vercel dashboard for production
# 4. Change the default admin password in db/seed.sql before running it
